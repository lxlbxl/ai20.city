// Tree-shaken Lucide icon set.
//
// `import { icons } from 'lucide'` pulls in the entire ~1,500-icon library
// (365KB) to render the handful actually used on the site. Add an icon here
// only when a new data-lucide="..." value is introduced elsewhere.
import { X, Check, ArrowUpRight, MapPin, Mail, Calendar, Linkedin, Twitter } from 'lucide';

export const icons = { X, Check, ArrowUpRight, MapPin, Mail, Calendar, Linkedin, Twitter };
